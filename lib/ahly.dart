import 'package:flutter/material.dart';

class fhfu extends StatelessWidget {
  const fhfu({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.cyan,
      ),
    );
  }
}
